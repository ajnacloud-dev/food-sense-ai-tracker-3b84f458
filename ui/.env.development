# Development Environment Variables

# API URL - Not needed in dev as we use Vite proxy
# VITE_API_URL is intentionally not set so the app uses relative URLs

# Auth mode
VITE_AUTH_MODE=local

# Feature flags
VITE_ENABLE_DEBUG=true
VITE_ENABLE_PWA=false